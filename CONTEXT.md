# CONTEXT — 厂区安全风险态势指挥大屏

> 本文件记录指挥大屏设计 grilling session 中已锁定的决策。
> 状态标记：✅已锁定 / 🔄讨论中 / ❌未决
> 项目位置：`/Users/ldh/Downloads/project/极客光年/drive-dashboard`
> 父仓：`/Users/ldh/Downloads/project/极客光年/bot`（管理端 + 实施端，数据契约源）

---

## 项目定位

| 项 | 决策 | 状态 |
|---|---|---|
| 项目归属 | 直接重构 drive-dashboard 仓库，继承其 git 历史，新增 CONTEXT 文档；不动 bot 仓库 | ✅ |
| 产品形态 | 自洽的指挥工作台，通过锁定/解锁切换姿态：锁定态=只读监控、解锁态=可操作指挥 | ✅ |
| 与 bot 关系 | 分工：大屏做指挥高频子集（查看+操作），bot 做配置管理面（路线/规则/点位/计划 CRUD/报表） | ✅ |

## 技术栈

| 项 | 决策 | 状态 |
|---|---|---|
| 框架 | Vue 3 + TypeScript + Vite | ✅ |
| UI | 自研 HUD，不用 Ant Design Vue | ✅ |
| 地图引擎 | Mapbox GL JS（WebGL 渲染，不做 Canvas 2D fallback） | ✅ |
| 3D 建筑物 | Mapbox fill-extrusion 单引擎，不引 Three.js overlay | ✅ |
| 状态管理 | Vue 3 reactive + composable（useMapState/useSelectedRobot/useAlerts 等），不用 Pinia | ✅ |
| 路由结构 | 单页无路由，全局态/聚焦态/锁定/解锁靠组件内部状态切换；选中机器人 ID 持久化到 localStorage `lastSelectedRobotId` | ✅ |
| 测试基建 | Vitest + happy-dom，只测 composable/utils 逻辑层，不测 UI 渲染 | ✅ |

## 数据源

| 项 | 决策 | 状态 |
|---|---|---|
| 数据契约 | 从 bot `src/types` 挑子集（Robot/Task/Alert/Exception/AuditLog/InspectionMap/NavigationPoint）复制到大屏作契约，不反向依赖 bot | ✅ |
| mock 载体 | localStorage，与 bot 同名共享 key（`robots`/`inspection-tasks`/`audit-log` 等） | ✅ |
| seed 数据 | 大屏自带 `src/mock/seed.ts`，首次启动 localStorage 空则灌入：1 地图、3-5 机器人（含低电/故障/巡检中）、2 路线、若干巡检点、3-5 告警、1 作业票、若干审计 | ✅ |
| 实时 mock | setInterval 驱动 mock 时钟，机器人按预设轨迹插值移动，告警按概率随机生成 | ✅ |
| 真实环境 | mockDataService 换成 realtimeService（WebSocket 订阅），上层组件不动 | ✅ |

## 组件树架构

```
App.vue                          ← 布局骨架 + isLocked + selectedRobotId
├── TopBar.vue                   ← 顶部状态栏（计数+时间+锁定按钮+图层开关）
├── MapStage.vue                 ← 中央地图（全局态主舞台）
├── MinimapOverlay.vue           ← 左下角缩略小地图（聚焦态显示）
├── FocusPanel.vue               ← 中央聚焦视频+属性（聚焦态显示）
│   ├── VideoFeed.vue            ← 双光视频
│   └── RobotPropertyCard.vue    ← 机器人属性摘要
├── Sidebar.vue                  ← 右侧栏
│   ├── GlobalOverview.vue       ← 上区：全局概览（未选中）
│   ├── PTZControl.vue           ← 上区：云台控制（选中+解锁）
│   ├── RobotList.vue            ← 下区：机器人列表（未选中）
│   └── RobotDetail.vue          ← 下区：机器人详情（选中）
├── AlertStream.vue              ← 底部告警事件流
├── ConfirmModal.vue             ← 通用二次确认弹窗（替代 PinModal）
├── AlertActionModal.vue         ← 告警处置弹窗（解锁态）
├── TakeoverModal.vue            ← 故障接管弹窗（解锁态）
└── DispatchModal.vue            ← 紧急派车弹窗（解锁态）
```

## 与 bot 的能力边界

| 能力 | 大屏 | bot | 备注 |
|---|---|---|---|
| 地图监控 | ✅ | ✅ | 大屏为大屏化呈现 |
| 视频控制（远控/云台） | ✅ 解锁态 | ✅ Console.vue | 大屏切入控制权后操作 |
| 告警处置 | ✅ 解锁态 | ✅ | 同源数据双入口 |
| 紧急派车/抢占/终止任务 | ✅ 解锁态 | ✅ | |
| 作业票触发/监护 | ✅ 解锁态 | ✅ ThirdPartySimulator | |
| 故障接管 | ✅ 解锁态 | ✅ DispatchCenter | |
| 审计查看 | ✅ 只读 | ✅ 留接口位 | 独立审计查看页归大屏 |
| 属性查看（点位/机器人/路线/排期/状态） | ✅ | ✅ | 大屏只读弹面板 |
| 路线编辑/点位管理/检测规则配置 | ❌ | ✅ | 配置归 bot |
| 计划 CRUD/资源基础配置/调度规则配置 | ❌ | ✅ | 配置归 bot |
| 报表导出 | ❌ | ✅ | 归 bot |

## 页面布局

| 区域 | 内容 | 状态 |
|---|---|---|
| 顶部窄状态栏 | 机器人/任务/告警计数 + 当前时间 + 锁定/解锁切换按钮 | ✅ |
| 中央主舞台 | Mapbox 地图（默认全局态），未选中机器人时占满中央 | ✅ |
| 中央聚焦态 | 选中机器人后，中央大区域显示双光视频+属性，地图缩到左下角小地图（机器人位置+告警锚点，点击放大回全局） | ✅ |
| 右侧栏 | 上下两区：上区=摄像机参数/PTZ 控制（解锁态可操作）、下区=机器人属性详情；顶部「返回全局」按钮清空选中 | ✅ |
| 底部 | 告警事件流滚动条 | ✅ |

### 右侧栏内容切换

| 子区 | 未选中（全局态） | 选中机器人（聚焦态） |
|---|---|---|
| 上区 | 厂区实时概览（小地图缩略/关键指标/今日告警 sparkline） | 摄像机参数/PTZ 控制（解锁态可操作，云台方位角/俯仰角/焦距/扫描模式） |
| 下区 | 机器人状态列表（一行一台，含电量/状态/当前任务，点击即选中） | 该机器人属性详情（位置/任务/检测数据/操作按钮） |
| 顶部 | — | 「返回全局」按钮，点击清空选中 |

### 选中机器人入口

- 点地图上机器人图标
- 点右侧机器人状态列表某行
- 点底部告警事件流某条告警（自动选中相关机器人）
- 故障接管弹窗里选目标机器人

## 地图图层

所有图层都保留，按优先级决定默认显隐：

| 优先级 | 图层 | 默认显示 | 数据源 |
|---|---|---|---|
| P0 | 底图 raster | ✅ | bot InspectionMap.imageUrl |
| P0 | 建筑物 3D extrusion | ✅ | 大屏 seed buildings GeoJSON（Polygon+height），bot 类型补 `buildings?: GeoJSON.FeatureCollection` |
| P0 | 路网 | ✅ | bot RoadSegment/RoadNode |
| P0 | 机器人实时位置 | ✅ | localStorage robots，实时更新 |
| P0 | 告警锚点 | ✅ | alerts/exceptions，爆闪脉冲动画 |
| P1 | 区域分区 | ✅ | bot AreaManage 区域 |
| P1 | 巡检点 | ✅ | bot InspectionPoint |
| P1 | 机器人轨迹 | ✅ | 实时生成+历史 |
| P1 | 视频视野锥 | ✅ 跟随选中机器人 | 机器人朝向+FOV 几何计算 |
| P2 | 导航点 | ❌ 默认隐 | bot NavigationPoint |
| P2 | 任务覆盖热力 | ❌ 默认隐 | 任务关联区域 |
| P2 | 作业票监护区 | ❌ 默认隐 | 作业票触发时临时高亮 |
| P2 | 漫游异常临时锚点 | ❌ 默认隐 | 边巡边检触发 |

### 图层控制

- 右上角折叠的图层开关面板（类似 Mapbox layer toggle）
- 锁定态下面板隐藏（不可改图层显隐）
- 解锁态下可手动开关 P2 图层

## 3D 建筑物实现

| 项 | 决策 | 状态 |
|---|---|---|
| 方案 | Mapbox fill-extrusion 单引擎，建筑物为挤出体块（无真实纹理） | ✅ |
| 建筑物数据 | 大屏 `seed.ts` 提供 3-5 个建筑物 Polygon（矩形）+ height 属性，灌入 localStorage | ✅ |
| map3d 项目 | 仅作思路参考，不用其 GLB 产物（OSM 无厂区私有数据） | ✅ |
| bot 类型扩展 | InspectionMap 补 `buildings?: GeoJSON.FeatureCollection<Polygon, {height:number}>` 可选字段，bot 不主动维护 | ✅ |

## 视频流

| 项 | 决策 | 状态 |
|---|---|---|
| 呈现 | 跟随选中机器人，选中后居中大屏中央显示双光视频（可见光+红外） | ✅ |
| 未选中 | 中央显示全局地图，不显示视频 | ✅ |
| mock 视频源 | `public/videos/` 放 2 段厂区巡检片段（可见光+红外，各 10-30s 循环），所有机器人共用，叠加不同 HUD 区分 | ✅ |
| 真实环境 | 换成 WebRTC/RTSP 流地址 | ✅ |
| 放大/全屏 | 视频支持点击放大全屏 | ✅ |
| 视频角落小地图 | 全屏视频时角落渲染小地图（机器人位置标注） | ✅ |

## 解锁态操作清单

- 告警处置：确认/误判/转隐患/转整改/推送第三方
- 机器人控制：获取电子控制权、切入远控、云台控制
- 任务调度：紧急派车、终止任务、抢占授权、作业票触发
- 故障接管：执行无线全量接管
- 审计查看：查看 audit-log（只读）
- 属性查看：点位/机器人/路线/排期/当前状态等只读弹面板

## 锁定/解锁机制

| 项 | 决策 | 状态 |
|---|---|---|
| 切换按钮位置 | 顶部状态栏 | ✅ |
| 锁定态限制 | 隐藏所有操作按钮+图层开关面板，纯态势展示 | ✅ |
| 解锁鉴权 | 原型 demo 阶段不做 PIN 码，改为二次确认弹窗：点「解锁」弹"确定进入操作态？"→确认即解锁；点「锁定」弹"确定锁定只读态？"→确认即锁定 | ✅ |
| 解锁后超时自动锁定 | 15 分钟无操作自动锁定，锁定前弹确认提示（非静默）；手动点「锁定」即时锁定 | ✅ |
| 组件 | PinModal.vue 改为 ConfirmModal.vue（通用二次确认弹窗，复用于其他确认场景） | ✅ |

## 待决问题

无（2026-06-24 grilling round 2 完成，全部锁定，见下方 grilling round 2 决策）。

---

## grilling round 2 决策（2026-06-24）

> 本轮覆盖 A 组（drive-dashboard CONTEXT 待决问题 5 项）+ C 组（CONTEXT 尚未展开的细节 3 项），共 8 项全部锁定。

### A 组：原待决问题

| # | 主题 | 决策 | 状态 |
|---|---|---|---|
| 36 | 告警锚点视觉规范 | 颜色按严重程度分级（紧急红/警告黄/信息蓝）+ 单圈脉冲动画 + 大小按严重程度分级（紧急大/警告中/信息小）+ 持续闪烁直到 ACK 后变灰 | ✅ |
| 37 | 机器人轨迹样式 | 双层：实时渐变线（最近 30 分钟，旧端淡→新端浓）+ 历史热力图（大屏 localStorage 独立累积），可切换显隐 | ✅ |
| 38 | 任务覆盖热力算法 | 巡检点覆盖率（已覆盖绿/未覆盖红），不做路段级热力（避免"路段频繁路过≠覆盖巡检点"的误导） | ✅ |
| 39 | 边巡边检锚点（原"漫游异常锚点"） | 更名为"边巡边检锚点"；视觉用三角警示形状 + 闪烁+虚线圈动画，与常规告警锚点（圆形脉冲分级色）完全错开 | ✅ |
| 40 | 部署方式 | mock 阶段 GitHub Pages（与 bot 一致），真实环境迁移内网服务器 | ✅ |

### C 组：CONTEXT 细节补充

| # | 主题 | 决策 | 状态 |
|---|---|---|---|
| 41 | mock 时钟节拍 | 机器人位置 200ms（5fps）；告警随机生成 30s 检查一次概率；电量衰减 30s 掉 1%；任务进度独立 1s 节拍（与位置更新解耦） | ✅ |
| 42 | seed 数据具体内容 | 全状态覆盖态：5 机器人（巡检中/待命/低电返充/故障待接管/充电中各 1），5 告警覆盖三级（紧急/警告/信息），1 作业票；机器人命名用角色式（如"北区巡检1号"） | ✅ |
| 43 | PIN 码安全策略 | 原型 demo 不做 PIN，用二次确认弹窗替代（见上方锁定/解锁机制段已同步更新） | ✅ |

---

## 关联文档

- bot 仓库 CONTEXT.md — 数据契约源
- bot 仓库 docs/prd/2026-06-23-7y-iteration-prd.md — 7 月迭代 PRD
- 知识库 02、工作任务/极客光年/机器人/功能与规划/7月迭代功能清单.md — 模块一法定范围
- 知识库 02、工作任务/极客光年/机器人/方案与设计/机器人7月迭代需求确认（2026-06-23）.md — 23 项 ADR
